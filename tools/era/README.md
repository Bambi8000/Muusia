# tools/era — historical one-shot scripts

Already-applied surgery, doc patches and era validators, kept for the
record. **Do not re-run**: anchored patch scripts are not idempotent,
and relative paths may no longer resolve from this subfolder.
Living tools (bake, extract, make-src-bundle, patch-docs, per-node
validators) stay in tools/.
